readme

## Adding background audio

Place `sandhanam.mp3` in the project's `public/` folder (root of dev server). The site will attempt to autoplay the looped background music but browsers may require a user interaction; use the play button in the bottom-right to start playback.